server {
    listen 80;
    listen [::]:80;
    server_name onedish.cyhao.space;

    location ^~ /.well-known/acme-challenge/ {
        root /www/acme;
        default_type text/plain;
        try_files $uri =404;
    }

    location / {
        return 503;
    }
}
